name: landops-workbench
metadata:
  template: landops-workbench@1.0.0

services:
  api:
    project: ./dotnet
    language: docker
    host: appservice
    docker:
      path: ./Dockerfile
      context: ..
      registry: ${AZURE_CONTAINER_REGISTRY_ENDPOINT}
      remoteBuild: true
